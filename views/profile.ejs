<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>CoderSTAT | Profile of <%= profileData.username %></title>
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.0.2/dist/tailwind.min.css" rel="stylesheet">
</head>
<body class="bg-gray-100 flex flex-col min-h-screen">
    <div class="container mx-auto mt-10 px-4 sm:px-6 lg:px-8">
        <div class="bg-white shadow-md rounded-lg p-6">
            <div class="text-center">
                <img src="<%= profileData.avatar %>" alt="Avatar" class="w-24 h-24 mx-auto rounded-full mb-4">
                <h1 class="text-2xl font-bold mb-2"><%= profileData.name %></h1>
                <p class="text-gray-600">@<%= profileData.username %></p>
                <p class="text-gray-600">Ranking: <%= profileData.ranking %></p>
                <p class="text-gray-600">Reputation: <%= profileData.reputation %></p>
                <p class="text-gray-600">Birthday: <%= profileData.birthday %></p>
                <p class="text-gray-600">Country: <%= profileData.country %></p>
                <p class="text-gray-600">School: <%= profileData.school %></p>
                <p class="text-gray-600">About: <%= profileData.about %></p>
                <p class="text-gray-600">Skills: <%= profileData.skillTags.join(', ') %></p>
                <div class="flex justify-center space-x-4 mt-4">
                    <a href="<%= profileData.gitHub %>" target="_blank" class="text-gray-500 hover:text-gray-900"><i class="fab fa-github"></i></a>
                    <a href="<%= profileData.twitter %>" target="_blank" class="text-blue-500 hover:text-blue-900"><i class="fab fa-twitter"></i></a>
                    <a href="<%= profileData.linkedIN %>" target="_blank" class="text-blue-700 hover:text-blue-900"><i class="fab fa-linkedin"></i></a>
                    <% profileData.website.forEach(site => { %>
                        <a href="<%= site %>" target="_blank" class="text-gray-500 hover:text-gray-900"><i class="fas fa-link"></i></a>
                    <% }) %>
                </div>
            </div>
        </div>
    </div>
</body>
</html>
